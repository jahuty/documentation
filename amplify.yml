version: 1
frontend:
  phases:
    preBuild:
      commands:
        - gem install bundler
        - bundle install --deployment --without development test
    build:
      commands:
        - bundle exec jekyll build
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'
  cache:
    paths: []
